import 'package:flutter/material.dart';

class TodayVotePeopleScreen extends StatelessWidget {
  const TodayVotePeopleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
